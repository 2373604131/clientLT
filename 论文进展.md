# 联邦长尾 CLIP 论文 · 进展主线文档

> 目标会场：CVPR。本文档记录**已验证有效**的 motivation、关键实验结论、以及我们一致同意的分析与下一步。
> 维护约定：只写站得住脚的内容；被推翻的结论要标注删除线并注明原因；每次实验有结果就回填。
> 最近更新：2026-08-07

---

## 0. 一句话故事线

> **问题不在于尾部知识能否被本地学到，而在于本地已经学到的尾部增益能否在全局聚合过程中存活。**

我们提出一个**客户端级长尾（client-level long-tail）数据划分**，它在保持全局类频率不变的前提下，独立操控"尾部证据由谁持有"这一**客户端拓扑维度**——这是主流迪利克雷划分无法刻画、但真实部署（医院、自动驾驶）本质具备的结构。在此划分下我们定位到 FedAvg 的**聚合坍缩机制**，并据此设计**拓扑感知的双路 LoRA 方法**。

---

## 1. Motivation（有效、可写进论文的）

### 1.1 为什么现有划分不够
- 主流联邦长尾论文仍用迪利克雷划分做数据切分。迪利克雷只打乱"每个客户端看到多少某类"，但**尾部证据被均匀撒在所有客户端上**。
- 真实世界不是这样：肿瘤医院＝尾部专科客户端（持有罕见病数据），综合医院＝头部客户端（常见病）。尾部知识**天然被少数专科客户端垄断**。
- 我们把长尾结构下沉到**客户端层面**，用严格数学参数控制，得到一个迪利克雷无法表达的新 setting。

### 1.2 两条正交的轴（论文的概念骨架）
- **类边际（class-marginal）**：类频率长尾。CAPT 等已有方法处理的就是这条轴。
- **客户端边际（client-marginal）**：客户端拓扑——**哪些客户端持有哪些类**。这是我们新增、且现有方法（含 CAPT）都**盲视**的轴。

### 1.3 为什么不跟 CAPT 正面刚
- CAPT 是 class-frequency-aware 但 **topology-blind**；它用自定义聚合（非 FedAvg）来维持尾部，但**有隐私泄露问题**，在极端 client-LT 划分下无法扫爆/公平对比。
- 结论（战略转向）：**不以"打败 CAPT"为卖点**，改以"客户端拓扑轴 + 聚合坍缩机制 + 拓扑感知 LoRA"立论。CAPT 退为 related-work/分析对象。

---

## 2. 关键实验（已完成，publication-strong）

> 基座：CIFAR100-LT（imb_factor=0.01，本身已全局长尾），PromptFL + FedAvg，30 客户端，本地 3 epoch，seed 42 & 2026。

### 2.1 Fig 1 —— 划分有效性（**不是**性能声明）
证明：**固定全局长尾**的前提下，我们能独立操控尾部证据的两个拓扑维度。
- `λ_T`（跨角色专精度）：把**同样的**尾部证据从普通客户端搬到 3 个尾部专科客户端。主设定 λ_T=0.75 → 专科质量 M_spec≈0.775，纯度 P_spec≈0.915。控制的是**谁持有尾部证据**，不是尾部样本数量。
- `α_T`（专科内 Dirichlet 浓度，越大越均匀/分散）：λ_T=0.75 时扫 α_T∈{0.1..1.0} → Top-2 质量 0.899→更低，有效客户端数 N_eff 1.26→2.07。
- 要点：全局长尾不变，被操控的只有客户端级拓扑。（"client-LT 更难"由 Fig 2 证明，不是这里。）

### 2.2 Fig 2 —— 尾部退化 + 聚合机制（核心）
- **(a) 尾部退化稳健性**：Bottom-20 尾部精度，client-LT vs 迪利克雷在**整条浓度扫描上都落后 16–20 pp**。→ 退化是稳健现象，不是单点 artifact。（单看 (a) 不能下因果拓扑结论。）
- **(b) 聚合质量证据**：支撑客户端的 FedAvg 总权重 client-LT **7.09%** vs 迪利克雷 **20.03%**；尾部有效客户端数 **2.53 vs 5.14**。→ 尾部知识被更少、质量更小的客户端携带。
- **(c) 分组赤字** ΔA = client-LT − 迪利克雷：尾部 **−19.15 pp**，非尾部 **−0.22 pp**，整体 **−4.00 pp**。→ 损伤高度**尾部特异**，不是普遍质量下降。
- **(d) 机制关键图 —— 反事实聚合分解**：尾部增益 `26.09 → 3.45 → 0.10`（client-LT） vs `23.51 → 8.86 → 1.73`（迪利克雷）。
  - **归一化支撑**（support 权重=1）：client-LT 26.09 > 迪利克雷 23.51 ⇒ **专科客户端确实学到了强尾部知识**（杀死"支撑客户端学不到尾部"的质疑）。
  - **恢复真实 FedAvg 权重**：26.09→3.45 = **稀释（dilution）**（呼应 (b) 的 7.09%）。
  - **加入全体客户端更新**：3.45→0.10 = **干扰（interference）**（头部更新覆盖尾部）。
  - → **两个物理上不同的失效** ⇒ 方法两个组件，各配一个消融。

### 2.3 补充诊断（已知冗余，勿重复投入）
- ~~`utils/aggregation_crush.py` 的 local-vs-global 探针~~：重新发明了一个更弱版的 Exp D(d)，且 `best_local`=max 有候选池偏差。**放弃，保留 Exp D**。若复活须改 `mean_local`。

---

## 3. 方法设计（当前版本 v3：双路 LoRA）

> 每个组件精确对应 Exp D(d) 的一支失效箭头。冻结 CLIP 双编码器 + 双路 LoRA。

| 失效箭头 | 机制 | 组件 |
|---|---|---|
| 26.09→3.45 稀释 | 质量加权压低尾部 | **可靠性门控聚合**：尾部残差只在支撑客户端上聚合，按拓扑可靠性标量 `R` 加权，**不按数据质量**。`R→0` ⇒ 残差→0 ⇒ 严格收缩回 zero-shot 锚点（安全）。 |
| 3.45→0.10 干扰 | 头部更新覆盖尾部 | **正交保护组合**：残差加在与冻结锚点、与共享 LoRA 张成都正交的子空间。`t_c' = Norm(t_c + Π_{t_c⊥}ρ_c)`，`Π_{t_c⊥}ρ = ρ − t_c(t_c·ρ)`。头部更新进不来。 |

- **共享路径**（头/中部类）：标准 LoRA + 正常 FedAvg + 非尾部 logit-adjusted CE。通用适配。
- **单一可靠性标量 `R`**（**不是**旧的 6-knob 几何均值）。一个可解释的拓扑可靠性值门控残差写入。

### 3.1 CV 落点（区别于纯 FL 论文的关键）
- **主域 = CLIP-weak 细粒度自然图像**（CUB 鸟类 / FGVC-Aircraft / 植物）；CIFAR-100-LT 仅作对照/sanity；**医学影像＝加分数据集**（呼应肿瘤医院 motivation）。
- 理由：在 CLIP-友好的 CIFAR 上尾部本就被 zero-shot 认识，"尾部知识"不是真的视觉学习问题（→ 会退回"数据受限"死路）。在 CLIP-weak 细粒度上，尾部类是**真正新的视觉概念**、天然由专科客户端持有 ⇒ (1) 尾部知识＝真 CV 学习，(2) 契合拓扑 motivation，(3) "打败 zero-shot / CAPT" 成为真声明。

### 3.2 让"视觉"可见（CVPR 高分必需）
1. **角度间隔** `m_c = <f̂,t̂_c> − max_{h∈head}<f̂,t̂_h>`：展示 FedAvg 把尾部推到 `m_c<0`（头部锚点吞掉尾部），方法修复到 `m_c>0`。把"头部吃尾部"变成可测图。
2. 锚点几何可视化：聚合后头部锚点锥扩张；残差补上正交的尾部判别方向。
3. **残差模态消融本身就是一个 CV 贡献**（已定：让实验决定，不预设）：残差加在视觉编码器 / 文本锚点 / 两者？cosine≈0.96 暗示文本锚点侧；CLIP-weak 细粒度可能需要视觉侧。这个张力就是干净的 CV 发现。
4. 展示残差编码了 CLIP 缺失的概念：只在 CLIP-weak 域存在的 zero-shot 增益被捕获**且**在聚合后存活。

### 3.3 被逼出来的基线（Fig 2d 直接递给审稿人）
既然归一化支撑一轮就恢复 26.09pp，审稿人**必问**："那你端到端跑 support-normalized FedAvg 会怎样？" **必须跑**，并展示它端到端要么方差爆炸/不稳定、要么毁掉头部。全文论证＝解释为什么这个简单聚合端到端失败、而门控+正交能赢。（这也反驳了"重加权无新意"——2d 的 26.09 证明单轮重加权**有效**，新意在于用 R 门控+正交保护**端到端**赢过它。）

---

## 4. 正在做的实验（实验2 · 先排两个死法）

> 顺序共识：**先做实验2 → 据结果回填文档/方法 → 再细化方法路线**。代码我负责，实验你跑。

### 4.1 (a) LoRA 底座上复现 dilution/interference
- 动机是 PromptFL，方法是 LoRA。**若两支箭头在 LoRA 上不重现，是硬伤。**
- 已完成代码：把 PromptFL 分支的 Exp D 钩子原样接到 `federated_main.py` 的 ClipLora 分支。Exp D 纯操作 full state_dict，冻结 CLIP delta=0，机器 trainer-无关，`experimentD_verify_fedavg` 会代数校验。
- **已修 bug（2026-08-07）**：ClipLora 联邦分支首次跑就报 `Missing key(s) ... q_proj.weight/v_proj.weight`（top3 块）。根因是 `utils/loralib/layers.py` 的 `LinearLoRA.forward` 用 merge→`delattr`/`setattr`→sub 的技巧，把冻结主权重 `weight` 从 nn.Parameter 降级成普通 tensor，导致它前向一次后就从 `state_dict` 消失，FedAvg 聚合缺键。修法：把该分支改成**等价的加法写法**（`W·x + (BA·scaling)·x`，与已有的 dropout>0 分支一致），主权重保持 Parameter 身份、数值不变。CLIP 仍冻结、只有 lora_A/lora_B 被训练与聚合。**未在本机跑数值验证（无 torch）**，属代数恒等，仍以 GPU 环境实跑为准。
- 运行：`--trainer ClipLora` + client-longtail + `--experimentD_enable True --experimentD_rounds 20,50,80`（LoRA 超参走默认 rank2/α1/top3/q,v/**encoder=vision**）。
- **看点**：`gain_support_actual`（纯尾部聚合）应 >> `gain_all`（全员 FedAvg），且 `acc_support_normalized` 能拉回大部分。重现＝方法底座一致；不重现＝方法要重设计。

### 4.2 (b) 尾部是否真·CLIP-weak —— **暂缓，先跑 (a)**
- 已完成代码：`scripts/probe_clip_weak_tail.py`——原始冻结 CLIP zero-shot 跑任意 ImageFolder，按精度排序，报尾部20%均值+判定（<40% 好落点 / >70% 死路）。
- **决策（2026-08-07）：现在不跑。** 理由：(1) 这道闸的答案很大程度能用已发表 CLIP zero-shot 数字预判，不必自己先跑；(2) 卡在需下载细粒度集。
- 文献预筛（ViT-B/16 近似 zero-shot，正式引用前须核对来源）：FGVC-Aircraft ~24%（很弱，好落点）/ CUB ~50-55%（边界，尾部更低）/ Stanford Cars ~65% / Flowers-102 ~71%（太友好）/ Food-101 ~86%（死路）。尾部20%会比均值更低。
- 何时真跑：**选定并下好候选集之后**跑一次，只为拿到"我们这个域+这个划分"的尾部 zero-shot 真实数字给审稿人。不阻塞当前。
- 可选：给探针加 CIFAR-100-LT 分支作反例对照（现成数据，预期尾部 zero-shot 偏高，佐证 CIFAR 只能当 sanity）。

### 4.3 第三个 make-or-break（待安排）
- **正交性能否挡住干扰**：带/不带正交约束消融，验证 3.45→0.10 这支箭头被阻断。

---

## 4.5 实验2(a) 结果（2026-08-07，CIFAR-100-LT + ClipLora + client-LT，seed42）

**核心发现：在 CIFAR 上 LoRA 微调对精度零贡献，尾部 66% 基本＝CLIP zero-shot。这是"CIFAR 不能当主战场"的铁证，不是失败。**

- **参数已核对齐**（partition_summary.json）：λ_T=0.75、α_T=0.5、head_leakage=3.0、3 个尾部专科客户端(27/28/29)、纯度 0.915、尾部有效客户端 2.43。划分与 motivation 主设定完全一致，切分无误。
- **LoRA 配置**（实际生效默认值）：r=2、alpha=1、position=top3、params=[q,v]、**encoder=vision（非 both，勘误 method-spec 3.2 曾写 both）**、dropout=0。是极小 LoRA，排除"容量过大跑成 trivial"。
- **决定性证据 —— 精度全程不动**：
  | epoch | tail(80-99) | head(0-79) |
  |---|---|---|
  | 0 | 66.55% | 64.80% |
  | 50 | 66.45% | 64.66% |
  | 99 | 66.50% | 64.54% |
  100 轮训练前后精度重合 → 微调对最终 argmax 无影响。
- **微调"动了却没用"**（要精确区分）：`client_update_norms.csv` 显示 A/B 一直在更新（mean_norm 0.096→0.26），梯度在流、参数没配错；但增量淹没在 CLIP 对 CIFAR 的强先验里，推不动预测。**且尾部专科更新范数仅为头部的 1/4**（0.06 vs 0.28）→ dilution 确实存在，只是被高 zero-shot 基线掩盖。
- **呼应 round_summary 三列全 0**：单轮反事实增益全 0 = (CIFAR 先验托底) + (单轮 LoRA 步长太小信噪比不足) 两因叠加，当前数据无法完全分离，但结论不受影响。

### 两条硬结论
1. **实锤"CIFAR 只能当 sanity"**：CLIP-友好域尾部知识 zero-shot 已具备，微调 100 轮零变化。真实"尾部=新视觉概念"的联邦长尾问题必须在 CLIP-weak 细粒度域研究。→ 审稿人质疑"凭什么弃 CIFAR"时的王牌图（训练前后精度重合线）。
2. **解释了 PromptFL 有强 crush、LoRA 没有**：不是机制消失，是 CIFAR 高 zero-shot 把动态压平；PromptFL 表达力弱+抢 prompt 才在训练中被头部拖下去。换 CLIP-weak 域，先验托不住底，微调必须真学，坍缩才会重现。

### 优先级调整
- CIFAR 上的 2(a) **已完成使命**（产出"反例对照"素材）。
- **真正胜负手 = 2(a')：在 CLIP-weak 域（Aircraft 首选）重跑同一套 client-LT + LoRA + Exp D。** 只有那里能验证 method-spec 两支箭头是否真实存在。
- **2(b)/下载 CLIP-weak 数据从"暂缓"升为最高优先** —— 没有它 2(a') 无法开跑，方法验证全卡在这。

## 5. 结果导向与决策树

- **【已发生】CIFAR 上 2(a) 精度零变化** → 不是机制被证伪，是 CIFAR zero-shot 先验托底压平了动态。产出"弃 CIFAR"的铁证。→ 必须搬到 CLIP-weak 域重跑（2(a')）。
- **2(a') 在 CLIP-weak 域重现坍缩** ✔ → 方法立得住 → 写文档1（正式方法 spec）+ 细化路线。
- **2(a') 在 CLIP-weak 域也平（微调仍无增益/无坍缩）** → 方法根基动摇 → 重新定位机制（换机制解释，或重审 method-spec 两支箭头）。
- **候选域尾部太 CLIP-友好** → 换更难的细粒度域，直到尾部 zero-shot 足够低（Aircraft ~24% 基本够）。
- 诚实风险（无 SOTA 保证）：死法都可预检——细粒度域太熟（增益太小）/ 正交挡不住干扰 / 被逼基线端到端已够好。上行：这个 niche（拓扑感知 + 保护 CLIP-weak 联邦长尾里真·新尾部视觉知识）是空的。

---

## 6. 待办清单

- [x] 实验2(a)：CIFAR + ClipLora + client-LT 跑 Exp D（seed 42）→ 精度零变化，产出"弃 CIFAR"铁证；结论见 4.5
- [ ] **【最高优先】下载 FGVC-Aircraft（首选，zero-shot ~24%）**，整理成 ImageFolder；跑 (b) 探针确认尾部 zero-shot 足够低
- [ ] **2(a')：在 Aircraft 上重跑 client-LT + LoRA + Exp D**，验证 method-spec 两支箭头是否真实存在（真正胜负手）
- [ ] 实验2(c)：正交约束消融，验证挡住 interference（在 CLIP-weak 域上，2(a') 确认坍缩后再做）
- [ ] 据实验2结果撰写文档1（方法 spec：数学形式、R 定义、正交组合、伪代码、消融表、被逼基线论证）
- [ ] 相机就绪：第三个 seed（当前 42、2026）
- [ ] 划分描述性统计（每尾类支撑数&质量份额分布），让审稿人看到两划分只在客户端拓扑上不同、全局频率匹配
- [ ] 基线清单落实：zero-shot CLIP、PromptFL、naive FedAvg-CLIP、FedRoD(frozen-CLIP)、federated-LoRA（FFA-LoRA/FedEx-LoRA，须核对来源）、**端到端 support-normalized FedAvg（被逼基线）**

---

## 附：术语速查
- **dilution 稀释**：质量加权把低质量客户端携带的尾部更新压小（26.09→3.45）。
- **interference 干扰**：头部客户端更新在决策边界覆盖尾部（3.45→0.10，cosine≈0.96 说明是边界挤压不是特征漂移）。
- **support clients 支撑客户端**：某尾部类样本数>0 的客户端。
- **R（可靠性标量）**：单一可解释的拓扑可靠性值，门控尾部残差写入。
- **client-longtail 划分参数**：λ_T 跨角色专精、α_T 专科内浓度、head/tail_client_ratio、head/tail_class_ratio。
