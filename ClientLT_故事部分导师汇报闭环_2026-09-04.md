# Client-LT 故事部分：导师汇报闭环

> 本版本只保留能够进入论文主线的正向证据，暂不讨论方法，也不回顾早期探索路线。

## 一句话主命题

在联邦长尾学习中，类别总样本量决定一个类别拥有多少证据，类别证据在客户端之间的分布决定这些证据能够以多大的直接刷新能力进入共享模型；当尾类证据集中在少数客户端时，模型仍能在训练早期获得较强尾类能力，但维护期的 supporter refresh 明显不足，最终使 rewrite 相对 positive refresh 占据主导，并产生持续的尾类退化。

---

## 1. 研究起点：联邦长尾包含两个结构维度

设联邦系统包含 $K$ 个客户端和 $C$ 个类别，客户端—类别样本矩阵为

\[
N\in\mathbb R^{K\times C},
\qquad N_{k,c}=\text{客户端 }k\text{ 中类别 }c\text{ 的样本数}.
\]

### 第一维：类别频率

类别 $c$ 的全局样本量为

\[
n_c=\sum_{k=1}^{K}N_{k,c}.
\]

经典长尾学习以及现有联邦长尾研究主要依据 $n_c$ 区分 head、medium 和 tail 类。这个维度描述每个类别在整个联邦系统中拥有多少训练样本。

### 第二维：类别证据的客户端暴露结构

给定相同的 $n_c$，这些样本仍然可以分散在大量客户端，也可以集中在少数客户端。类别 $c$ 在客户端之间的暴露分布可以写为

\[
p(k\mid c)=\frac{N_{k,c}}{n_c}.
\]

它描述一个类别的证据由哪些客户端持有、集中程度多高，以及这些持有者在联邦聚合中能够提供多少直接更新。

因此，联邦长尾数据由两个相互独立的结构共同决定：

\[
\boxed{
\text{class-frequency imbalance}
+
\text{class-conditioned client exposure}
}
\]

这一观察构成全文的问题入口：仅知道 $n_c$，还无法完整判断一个尾类在联邦训练中的学习状态。

---

## 2. 严格控制：固定两个边际，只改变 client–class coupling

为了单独研究第二个维度，我们构造两种 matched topology：

- Client-LT：尾类证据集中在较少的 specialist clients；
- matched Dirichlet：相同尾类证据分布得更广。

两种条件严格保持：

\[
n_c^{\mathrm{ClientLT}}=n_c^{\mathrm{Dir}},
\qquad
n_k^{\mathrm{ClientLT}}=n_k^{\mathrm{Dir}}.
\]

同时保持：

- 每个类别的全局样本量相同；
- 每个客户端的总样本量相同；
- 总训练样本均为 10,847；
- FedAvg 聚合权重逐客户端完全相同；
- 有效聚合客户端数均为 26.729；
- 模型、初始化、优化器、训练轮次和参与率完全一致。

实验中唯一变化的是客户端—类别矩阵内部的 coupling，即 $p(k\mid c)$。

### 拓扑变化确实发生

| 尾类拓扑指标 | Client-LT | matched Dirichlet |
|---|---:|---:|
| 平均 supporter 数 | 3.65 | 5.35 |
| 最大单客户端证据占比 | 59.14% | 34.39% |
| 有效支持客户端数 | 2.43 | 4.56 |
| 最后三个小客户端的 FedAvg 总权重 | 1.198% | 1.198% |

这些统计说明：在两个边际和 FedAvg 权重完全相同的条件下，Client-LT 将相同数量的尾类证据集中到了更少的客户端上。

由此得到故事的第一个实证环节：

\[
\boxed{
\text{fixed marginals}
\;\xrightarrow{\;p(k\mid c)\;\text{changed}\;}
\text{tail-evidence concentration}
}
\]

---

## 3. 核心现象：两种拓扑具有相同的早期学习能力，却产生不同的维护轨迹

在 CIFAR-100-LT、IF=100、CLIP ViT-B/16、vision LoRA、FedAvg、30 clients、3 local epochs、100 communication rounds、full participation 的条件下，两种拓扑的训练前期几乎重合。

### 训练结果

| 指标 | Client-LT | matched Dirichlet |
|---|---:|---:|
| 最佳整体准确率 | 67.48 | 67.54 |
| 最终整体准确率 | 64.79 | 67.00 |
| 最佳尾类准确率 | 68.80 | 68.75 |
| 最终尾类准确率 | 37.15 | 49.40 |
| 尾类 best-to-final drop | 31.65 | 19.35 |
| 最终头类准确率 | 71.70 | 71.40 |

两种拓扑都能够在前 5 轮左右将尾类准确率提升到约 68.8%。这表明集中持有尾类证据的 specialist clients 能够形成有效的尾类适配，Client-LT 的早期 acquisition 能力保持完整。

从第 10 轮开始，两条曲线逐步分离：

| Round | Client-LT tail | Dirichlet tail | Client-LT − Dirichlet |
|---:|---:|---:|---:|
| 5 | 68.80 | 68.75 | +0.05 |
| 10 | 58.95 | 60.05 | −1.10 |
| 20 | 42.05 | 49.20 | −7.15 |
| 30 | 37.25 | 47.10 | −9.85 |
| 60 | 37.35 | 49.65 | −12.30 |
| 90 | 36.80 | 50.45 | −13.65 |
| 100 | 37.15 | 49.40 | −12.25 |

Client-LT 最终尾类准确率低 12.25 个百分点，而最终头类准确率高 0.30 个百分点。性能差异集中在尾类维护阶段，整体训练和头类学习保持稳定。

由此得到第二个实证环节：

\[
\boxed{
\text{same early tail acquisition}
\;\longrightarrow\;
\text{different long-term maintenance}
}
\]

---

## 4. 类别级一致性：退化覆盖整个尾类集合

为了确认现象是否由少数异常类别造成，我们进一步计算每个尾类各自的 best-to-final drop。

| Per-class 指标 | Client-LT | matched Dirichlet |
|---|---:|---:|
| 平均 BFD | 33.25 | 21.05 |
| 中位数 BFD | 34.50 | 19.50 |
| BFD > 20 的类别数 | 15/20 | 10/20 |
| 平均 log-odds BFD | 2.371 | 1.358 |
| 最终平均 log-odds | −1.661 | −0.521 |

最关键的类别级结果是：

\[
\boxed{
20/20\text{ 个尾类在 Client-LT 下的 BFD 均高于 matched Dirichlet}
}
\]

Client-LT 相对 matched Dirichlet 的平均 per-class BFD 增量为 12.20 个百分点。拓扑效应在全部尾类上具有一致方向。

由此得到第三个实证环节：

\[
\boxed{
\text{topology effect is systematic across tail classes}
}
\]

---

## 5. 从训练轨迹进入机制：共享模型中的三类功能作用

所有客户端都更新同一个视觉 LoRA，并通过 100 类全局 logits 共同改变共享表示。对于某个尾类 $c$，每个客户端更新对该类的功能影响可以按照“是否持有该类证据”和“作用符号”划分为三部分。

### 5.1 Supporter write

持有类别 $c$ 样本的客户端，利用直接监督对该类产生的正向功能写入记为

\[
W_c.
\]

它表示尾类证据通过真实 supporter 进入共享模型的直接刷新能力。

### 5.2 Donor effect

没有类别 $c$ 样本的客户端，其更新仍可能改善类别 $c$ 的功能，这部分正向作用记为

\[
D_c.
\]

这说明共享表示中的跨类迁移能够为尾类提供额外正向刷新。

### 5.3 Destructive rewrite

没有类别 $c$ 样本的客户端也可能降低类别 $c$ 的 margin，这部分负向作用记为

\[
R_c.
\]

因此，类别 $c$ 在维护期获得的总正向刷新为

\[
P_c=W_c+D_c.
\]

我们使用两个比率描述正向刷新与负向改写之间的相对平衡：

\[
\mathrm{CERI}_c=\frac{R_c}{P_c},
\qquad
\mathrm{ERIS}_c=\frac{R_c}{R_c+P_c}.
\]

当 ERIS 高于 0.5 时，表示维护期内 destructive rewrite 相对 positive refresh 占据主导。

---

## 6. 机制结果：Client-LT 降低了直接刷新，使 rewrite/refresh balance 发生反转

基于 15 个预注册审计轮和逐客户端路径积分归因，我们得到：

| 维护期指标 | Client-LT | matched Dirichlet | 拓扑变化 |
|---|---:|---:|---:|
| supporter 写入 $W$ | 6.874 | 14.553 | −52.8% |
| donor $D$ | 17.642 | 14.646 | +20.5% |
| 总正向刷新 $P=W+D$ | 24.516 | 29.200 | −16.0% |
| destructive rewrite $R$ | 25.612 | 28.353 | −9.7% |
| CERI $R/P$ | 1.0458 | 0.9720 | 由低于 1 变为高于 1 |
| ERIS $R/(R+P)$ | 0.5106 | 0.4921 | 由低于 0.5 变为高于 0.5 |

这组结果揭示了完整的动态过程：

1. Client-LT 中的 supporter 写入下降 52.8%，构成正向刷新缺口的主要来源；
2. class-absent donor 提供了更多正向作用，说明共享更新中存在有效的跨类迁移；
3. donor 的增加未能补足 supporter write 的下降，总正向刷新 $P$ 仍低 16.0%；
4. destructive rewrite 的绝对量同时下降，但下降幅度小于 positive refresh；
5. 最终 CERI 从 0.9720 上升到 1.0458，ERIS 从 0.4921 上升到 0.5106，维护状态跨过平衡点。

因此，Client-LT 造成的是一个相对动态失衡：相同的尾类知识能够在早期进入模型，但少量 supporter 难以在后续训练中持续提供与共享改写相匹配的正向刷新。

由此得到第四个实证环节：

\[
\boxed{
\text{tail-evidence concentration}
\rightarrow W\downarrow
\rightarrow P\downarrow
\rightarrow \frac{R}{P}\uparrow
}
\]

---

## 7. 指标与退化的对应：ERIS 能够解释类别级维护失败

类别级结果进一步支持这一机制：

- 20 个尾类中，有 14 个类别在 Client-LT 下具有更高 ERIS；
- seed42 的 paired-class 检验为 $p=0.0073$；
- Client-LT 中，ERIS 与 margin BFD 的 Spearman 相关为 $\rho=0.492,\ p=0.0277$；
- matched Dirichlet 中，该相关为 $\rho=0.535,\ p=0.0150$；
- Client-LT 的平均 margin drop 为 2.264，matched Dirichlet 为 1.183；
- Client-LT 的 final-5 tail accuracy 为 37.16%，matched Dirichlet 为 50.05%。

这说明 ERIS 描述的 refresh–rewrite balance 与类别在维护期的 margin 退化具有一致关系：一个尾类的 rewrite 相对 positive refresh 越强，其 best-to-final degradation 越大。

归因计算本身也通过了完整性验证：

- 最大路径积分闭合误差低于 $4.8\times10^{-6}$；
- 一阶近似与路径积分的 client-effect sign agreement 分别达到 99.17% 和 98.93%；
- median Spearman 均为 0.9991；
- 两种归因方式得到的 topology contrast 基本一致。

由此得到第五个实证环节：

\[
\boxed{
\mathrm{ERIS}_c\uparrow
\quad\Longrightarrow\quad
\mathrm{BFD}_c\text{ tends to increase}
}
\]

---

## 8. 完整故事闭环

整篇论文的故事可以收束为以下七步：

### Step 1：联邦长尾具有两个维度

类别总频率 $n_c$ 描述证据数量，类别条件客户端暴露 $p(k\mid c)$ 描述证据由谁持有以及分布得多广。

### Step 2：固定边际后，拓扑仍然可以独立变化

在完全固定 $n_c$、$n_k$、FedAvg 权重和训练预算的情况下，Client-LT 将尾类证据集中在更少的 supporter 上。

### Step 3：拓扑变化产生独立的尾类性能差异

两种拓扑具有几乎相同的 early peak，但 Client-LT 在维护期下降更快，最终形成 12.25 个百分点的 tail gap。

### Step 4：这一差异是系统性的

20/20 个尾类在 Client-LT 中都表现出更大的 best-to-final drop，说明现象覆盖整个尾类集合。

### Step 5：尾类维护由正向刷新和共享改写共同决定

正向刷新来自 supporter write $W$ 和 class-absent donor $D$，负向作用来自 destructive rewrite $R$。

### Step 6：Client-LT 改变了三者的相对平衡

Client-LT 使 $W$ 大幅下降；$D$ 提供部分补偿；总正向刷新 $P$ 的下降超过 $R$ 的下降，最终使 ERIS 跨过 0.5。

### Step 7：相对失衡转化为尾类维护失败

更高 ERIS 与更大的类别 margin BFD 相对应，形成从数据拓扑、训练动力学到最终尾类退化的完整证据链。

全文机制链可以压缩为：

\[
\boxed{
\begin{aligned}
&\text{tail evidence concentrates on fewer clients}\\
&\Longrightarrow \text{direct supporter refresh }W\text{ becomes scarce}\\
&\Longrightarrow \text{total positive refresh }P=W+D\text{ decreases}\\
&\Longrightarrow \text{rewrite dominates relative to refresh}\\
&\Longrightarrow \mathrm{ERIS}>0.5\\
&\Longrightarrow \text{larger tail margin and accuracy degradation.}
\end{aligned}
}
\]

---

## 9. 最终核心命题

> 联邦长尾学习的难度同时取决于类别拥有多少证据，以及这些证据由多少客户端持续写入共享模型。Client-LT 将尾类证据集中在少数 supporter 上，使尾类能够在早期形成，却在维护期缺少足够的直接正向刷新。class-absent clients 同时产生 donor 与 destructive rewrite；donor 只能部分补偿 supporter write 的下降，最终使 rewrite 相对 positive refresh 占据主导，并导致系统性的尾类 best-to-final degradation。

论文可以将这一机制简洁概括为：

\[
\boxed{
\text{Evidence–Rewrite Imbalance}
}
\]

中文表述为：

> **证据—改写失衡。**

---

## 10. 给老师汇报时可以直接讲的一段话

现有联邦长尾学习主要依据一个类别的全局样本量来判断它是 head 还是 tail，但在联邦系统中，相同数量的类别样本还可以分散在很多客户端，也可以集中在少数客户端。因此，我们把类别频率和类别在客户端之间的暴露结构看作联邦长尾的两个维度。为了单独研究第二个维度，我们严格固定每个类别的样本量、每个客户端的数据量、FedAvg 权重和全部训练配置，只改变类别与客户端之间的 coupling。结果显示，Client-LT 将尾类证据集中到更少的 supporter 上；两种拓扑在训练早期都能把尾类准确率提升到约 68.8%，但随后 Client-LT 的尾类性能持续下降，最终比 matched Dirichlet 低 12.25 个百分点，而且 20 个尾类全部表现出更大的 best-to-final drop。进一步的逐客户端功能归因表明，Client-LT 中 supporter 对尾类的直接正向写入下降了 52.8%；其他客户端产生的 donor 作用有所增加，但不足以补偿这一缺口，因此总正向刷新下降了 16%。与此同时，destructive rewrite 的下降幅度只有 9.7%，使 CERI 和 ERIS 分别跨过 1 和 0.5 的平衡点。类别级上，ERIS 越高，margin 的 best-to-final drop 越大。由此，我们得到完整闭环：尾类证据集中减少了持续的 supporter refresh，使共享改写相对正向刷新逐渐占优，最终造成尾类能够形成、却难以在全局模型中长期维持。

---

## 11. 汇报页面顺序

| 页码 | 页面标题 | 这一页只讲什么 |
|---:|---|---|
| 1 | 联邦长尾的两个维度 | $n_c$ 描述数量，$p(k\mid c)$ 描述证据位置 |
| 2 | 固定边际的拓扑对照 | 两边只改变 client–class coupling |
| 3 | Client-LT 确实集中尾类证据 | supporter、最大质量占比、有效客户端数 |
| 4 | 相同峰值，不同维护轨迹 | early peak 相同，后期 tail gap 逐渐扩大 |
| 5 | 退化覆盖全部尾类 | 20/20 per-class BFD 更大 |
| 6 | 三类功能作用 | supporter write、donor、destructive rewrite |
| 7 | 维护期归因结果 | $W,D,P,R,\mathrm{CERI},\mathrm{ERIS}$ 主表 |
| 8 | ERIS 对应类别级退化 | paired test、Spearman、margin BFD |
| 9 | 最终机制闭环 | topology → refresh scarcity → imbalance → degradation |

---

## 12. 故事一致性检查

- **问题到实验变量：通过。** 第二维被严格操作化为固定边际后的 $p(k\mid c)$。
- **实验变量到现象：通过。** Client-LT 产生更集中的尾类证据和更大的维护期退化。
- **现象到机制量：通过。** 逐客户端归因把维护过程分解为 $W,D,R$，并得到总正向刷新 $P$。
- **机制量到最终结果：通过。** ERIS 的拓扑差异、类别级检验及其与 margin BFD 的相关共同连接机制与结果。

当前故事主体已经形成闭环。正在进行的多 seed 重复将进一步确认 ERIS 拓扑差异及类别级相关的统计泛化性。
